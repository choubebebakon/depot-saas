import { useMemo, useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { AlertTriangle, CheckCircle2, ClipboardList, Eye, FilePlus2, PackageCheck, Plus, RefreshCw, Search, Send, ShoppingCart, Trash2, X } from 'lucide-react';
import { useAuth } from '../../../contexts/AuthContext';
import { useDepot } from '../../../contexts/DepotContext';
import { useNotif } from '../../../context/NotifContext';
import { achatsApi } from '../services/achatsApi';
import './AchatsReceptionsPage.css';

const UNITS = ['PIECE', 'BOUTEILLE', 'CASIER', 'PACK', 'PALETTE', 'PLATEAU'];
const PAYMENTS = [{ value: 'CASH', label: 'Espèces' }, { value: 'OM', label: 'Orange Money' }, { value: 'MOMO', label: 'MTN MoMo' }, { value: 'CREDIT', label: 'Crédit' }];
const STATUS = { BROUILLON: 'Brouillon', ENVOYE: 'Envoyée', RECU: 'Reçue', ANNULE: 'Annulée' };
const statusClass = { BROUILLON: 'bg-slate-700/60 text-slate-200 border-slate-600', ENVOYE: 'bg-blue-500/10 text-blue-300 border-blue-500/30', RECU: 'bg-emerald-500/10 text-emerald-300 border-emerald-500/30', ANNULE: 'bg-red-500/10 text-red-300 border-red-500/30' };
const list = (v) => Array.isArray(v) ? v : Array.isArray(v?.data) ? v.data : Array.isArray(v?.items) ? v.items : [];
const money = (v) => `${Number(v || 0).toLocaleString('fr-FR', { maximumFractionDigits: 2 })} FCFA`;
const key = () => globalThis.crypto?.randomUUID?.() || `reception-${Date.now()}-${Math.random().toString(36).slice(2)}`;
const ref = () => `CMD-${new Date().toISOString().replace(/[-:TZ.]/g, '').slice(0, 14)}-${Math.random().toString(36).slice(2, 7).toUpperCase()}`;

function Button({ children, icon: Icon, variant = 'secondary', ...props }) { const s = { primary: 'bg-blue-600 hover:bg-blue-500 text-white shadow-lg shadow-blue-600/20', secondary: 'bg-slate-800 hover:bg-slate-700 text-slate-100 border border-slate-700', ghost: 'bg-transparent hover:bg-slate-800 text-slate-300', success: 'bg-emerald-600 hover:bg-emerald-500 text-white', warning: 'bg-amber-600 hover:bg-amber-500 text-white' }; return <button type="button" className={`inline-flex items-center justify-center gap-2 rounded-xl px-3.5 py-2.5 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-50 ${s[variant]}`} {...props}>{Icon && <Icon size={16} />}{children}</button>; }
function Field({ label, children }) { return <label className="block"><span className="mb-1.5 block text-xs font-semibold text-slate-400">{label}</span>{children}</label>; }
function Modal({ title, icon: Icon, onClose, children, wide = false }) { return <div className="fixed inset-0 z-[80] flex items-center justify-center bg-slate-950/75 p-4 backdrop-blur-sm"><div className={`max-h-[92vh] w-full overflow-hidden rounded-2xl border border-slate-700 bg-slate-900 shadow-2xl ${wide ? 'max-w-5xl' : 'max-w-2xl'}`}><div className="flex items-center justify-between border-b border-slate-800 px-5 py-4"><div className="flex items-center gap-3"><div className="rounded-xl bg-blue-500/10 p-2 text-blue-300"><Icon size={19} /></div><h2 className="font-bold text-white">{title}</h2></div><button type="button" onClick={onClose} className="rounded-lg p-2 text-slate-500 hover:bg-slate-800 hover:text-white"><X size={18} /></button></div><div className="max-h-[calc(92vh-72px)] overflow-y-auto p-5">{children}</div></div></div>; }
function Summary({ label, value, emphasis = false }) { return <div><p className="text-xs text-slate-500">{label}</p><p className={`mt-1 text-lg font-black ${emphasis ? 'text-amber-300' : 'text-white'}`}>{value}</p></div>; }
function Empty({ icon: Icon, title, text }) { return <div className="p-12 text-center"><Icon className="mx-auto mb-3 text-slate-600" size={30} /><p className="font-semibold text-slate-300">{title}</p><p className="mt-1 text-sm text-slate-500">{text}</p></div>; }

export default function AchatsReceptionsPageV2() {
  const { user } = useAuth(); const { depotId, depotActif } = useDepot(); const notif = useNotif(); const qc = useQueryClient();
  const [tab, setTab] = useState('commandes'); const [search, setSearch] = useState(''); const [modal, setModal] = useState(null); const [selected, setSelected] = useState(null);
  const [cmd, setCmd] = useState({ fournisseurId: '', note: '', lignes: [] });
  const [rec, setRec] = useState({ fournisseurId: '', modePaiement: 'CASH', montantPaye: 0, numBordereau: '', note: '', lignes: [] });
  const enabled = Boolean(depotId);
  const suppliers = useQuery({ queryKey: ['achats-fournisseurs', depotId], queryFn: achatsApi.getFournisseurs, enabled, staleTime: 30000 });
  const articles = useQuery({ queryKey: ['achats-articles', depotId], queryFn: achatsApi.getArticles, enabled, staleTime: 30000 });
  const commands = useQuery({ queryKey: ['achats-commandes', depotId], queryFn: achatsApi.getCommandes, enabled, staleTime: 15000 });
  const receptions = useQuery({ queryKey: ['achats-receptions', depotId], queryFn: achatsApi.getReceptions, enabled, staleTime: 15000 });
  const suggestions = useQuery({ queryKey: ['achats-suggestions', depotId], queryFn: achatsApi.getSuggestions, enabled, staleTime: 30000 });
  const fournisseurs = useMemo(() => list(suppliers.data), [suppliers.data]); const articleList = useMemo(() => list(articles.data), [articles.data]); const commandList = useMemo(() => list(commands.data), [commands.data]); const receptionList = useMemo(() => list(receptions.data), [receptions.data]); const suggestionList = useMemo(() => list(suggestions.data), [suggestions.data]);
  const term = search.trim().toLowerCase();
  const filteredCommands = commandList.filter((c) => !term || [c.reference, c.statut, c.fournisseur?.nom].filter(Boolean).some((v) => String(v).toLowerCase().includes(term)));
  const filteredReceptions = receptionList.filter((r) => !term || [r.reference, r.numBordereau, r.fournisseur?.nom].filter(Boolean).some((v) => String(v).toLowerCase().includes(term)));
  const invalidate = () => ['achats-commandes', 'achats-receptions', 'achats-suggestions', 'achats-fournisseurs', 'achats-articles', 'depot-dashboard', 'depot-stock'].forEach((k) => qc.invalidateQueries({ queryKey: [k, depotId] }));
  const createCommand = useMutation({ mutationFn: achatsApi.createCommande, onSuccess: () => { invalidate(); setModal(null); notif.success('Commande fournisseur créée.'); }, onError: (e) => notif.error(e?.response?.data?.message || 'Impossible de créer la commande.') });
  const updateCommand = useMutation({ mutationFn: ({ id, payload }) => achatsApi.updateCommande(id, payload), onSuccess: () => { invalidate(); setModal(null); notif.success('Commande mise à jour.'); }, onError: (e) => notif.error(e?.response?.data?.message || 'Impossible de mettre à jour la commande.') });
  const statusMutation = useMutation({ mutationFn: ({ id, status }) => achatsApi.updateCommandeStatut(id, status), onSuccess: () => { invalidate(); notif.success('Statut de la commande mis à jour.'); }, onError: (e) => notif.error(e?.response?.data?.message || 'Transition refusée.') });
  const createReception = useMutation({ mutationFn: ({ payload, key: idem }) => achatsApi.createReception(payload, idem), onSuccess: () => { invalidate(); setModal(null); notif.success('Réception enregistrée. Stock et dette fournisseur mis à jour.'); }, onError: (e) => notif.error(e?.response?.data?.message || 'Impossible d’enregistrer la réception.') });
  const isPatron = user?.role === 'PATRON' || user?.isSuperAdmin === true;
  const add = (setter, line) => setter((f) => ({ ...f, lignes: [...f.lignes, line] })); const update = (setter, i, k, v) => setter((f) => ({ ...f, lignes: f.lignes.map((l, x) => x === i ? { ...l, [k]: v } : l) })); const remove = (setter, i) => setter((f) => ({ ...f, lignes: f.lignes.filter((_, x) => x !== i) }));
  const submitCommand = () => { if (!cmd.fournisseurId || !cmd.lignes.length) return notif.error('Sélectionnez un fournisseur et au moins un article.'); const ids = cmd.lignes.map((l) => l.articleId); if (cmd.lignes.some((l) => !l.articleId || !Number.isInteger(Number(l.quantite)) || Number(l.quantite) < 1 || Number(l.prixAchatUnit) < 0)) return notif.error('Vérifiez les lignes de la commande.'); if (new Set(ids).size !== ids.length) return notif.error('Un article ne peut apparaître qu’une seule fois dans une commande.'); createCommand.mutate({ reference: ref(), fournisseurId: cmd.fournisseurId, note: cmd.note.trim() || undefined, lignes: cmd.lignes.map((l) => ({ articleId: l.articleId, quantite: Number(l.quantite), prixAchatUnit: Number(l.prixAchatUnit) })) }); };
  const total = rec.lignes.reduce((s, l) => s + Number(l.quantiteLivree || 0) * Number(l.prixAchatUnitaire || 0), 0); const debt = Math.max(0, total - Number(rec.montantPaye || 0));
  const submitReception = () => { if (!rec.fournisseurId || !rec.lignes.length) return notif.error('Sélectionnez un fournisseur et au moins un article.'); const ids = rec.lignes.map((l) => l.articleId); if (rec.lignes.some((l) => !l.articleId || !Number.isInteger(Number(l.quantiteLivree)) || Number(l.quantiteLivree) < 1 || !Number.isInteger(Number(l.quantiteGratuite)) || Number(l.quantiteGratuite) < 0 || Number(l.prixAchatUnitaire) < 0)) return notif.error('Vérifiez les lignes de la réception.'); if (new Set(ids).size !== ids.length) return notif.error('Un article ne peut apparaître qu’une seule fois dans une réception.'); const paid = Number(rec.montantPaye || 0); if (paid > total) return notif.error('Le montant payé ne peut pas dépasser le total.'); if (rec.modePaiement === 'CREDIT' && paid !== 0) return notif.error('Une réception en crédit ne peut pas avoir de paiement immédiat.'); createReception.mutate({ key: key(), payload: { fournisseurId: rec.fournisseurId, modePaiement: rec.modePaiement, montantPaye: paid, lignes: rec.lignes.map((l) => ({ articleId: l.articleId, quantiteLivree: Number(l.quantiteLivree), quantiteGratuite: Number(l.quantiteGratuite || 0), prixAchatUnitaire: Number(l.prixAchatUnitaire), unite: l.unite })), numBordereau: rec.numBordereau.trim() || undefined, note: rec.note.trim() || undefined } }); };
  if (!enabled) return <div className="p-8"><div className="rounded-2xl border border-amber-500/30 bg-amber-500/10 p-6 text-center"><AlertTriangle className="mx-auto mb-3 text-amber-300" size={28} /><h2 className="font-bold text-white">Aucun dépôt actif</h2><p className="mt-1 text-sm text-slate-400">Sélectionnez un dépôt avant de gérer les achats et réceptions.</p></div></div>;
  const loading = suppliers.isLoading || articles.isLoading || commands.isLoading || receptions.isLoading;
  return <div className="space-y-6 p-4 sm:p-6"><div className="flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between"><div><div className="flex items-center gap-2 text-blue-300"><ShoppingCart size={18} /><span className="text-xs font-bold uppercase tracking-[0.18em]">Achats & réceptions</span></div><h1 className="mt-1 text-2xl font-black text-white">Approvisionnements</h1><p className="mt-1 text-sm text-slate-400">Dépôt actif : <span className="font-semibold text-slate-200">{depotActif?.nom || 'Dépôt sélectionné'}</span></p></div><div className="flex gap-2"><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-500" size={16} /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Rechercher…" className="control pl-9 sm:w-72" /></div><Button icon={RefreshCw} onClick={invalidate} disabled={loading}>Actualiser</Button></div></div>
  <div className="grid grid-cols-1 gap-3 md:grid-cols-3"><Summary label="Commandes" value={commandList.length} /><Summary label="Réceptions" value={receptionList.length} /><Summary label="Articles à réapprovisionner" value={suggestionList.length} emphasis /></div>
  {suggestionList.length > 0 && <div className="rounded-2xl border border-amber-500/20 bg-amber-500/5 p-4"><div className="mb-3 flex items-center justify-between gap-3"><div><h2 className="font-bold text-white">Suggestions de réapprovisionnement</h2><p className="text-xs text-slate-400">Stock sous le seuil critique du dépôt actif.</p></div><Button variant="ghost" icon={FilePlus2} onClick={() => { setTab('commandes'); setCmd({ fournisseurId: fournisseurs[0]?.id || '', note: '', lignes: [] }); setModal('new-command'); }}>Créer une commande</Button></div><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{suggestionList.slice(0, 8).map((s) => <div key={s.articleId} className="rounded-xl border border-slate-800 bg-slate-900/60 p-3"><p className="truncate text-sm font-semibold text-white">{s.designation}</p><p className="mt-1 text-xs text-slate-500">Stock {s.quantiteActuelle} / seuil {s.seuilCritique}</p></div>)}</div></div>}
  <div className="flex flex-wrap items-center justify-between gap-3 border-b border-slate-800"><div className="flex gap-1"><button type="button" onClick={() => setTab('commandes')} className={`border-b-2 px-4 py-3 text-sm font-bold ${tab === 'commandes' ? 'border-blue-500 text-white' : 'border-transparent text-slate-500'}`}>Commandes fournisseurs</button><button type="button" onClick={() => setTab('receptions')} className={`border-b-2 px-4 py-3 text-sm font-bold ${tab === 'receptions' ? 'border-emerald-500 text-white' : 'border-transparent text-slate-500'}`}>Réceptions</button></div><Button variant={tab === 'commandes' ? 'primary' : 'success'} icon={tab === 'commandes' ? FilePlus2 : PackageCheck} onClick={tab === 'commandes' ? () => { setCmd({ fournisseurId: fournisseurs[0]?.id || '', note: '', lignes: [] }); setModal('new-command'); } : () => { setRec({ fournisseurId: fournisseurs[0]?.id || '', modePaiement: 'CASH', montantPaye: 0, numBordereau: '', note: '', lignes: [] }); setModal('new-reception'); }}>{tab === 'commandes' ? 'Nouvelle commande' : 'Nouvelle réception'}</Button></div>
  {loading ? <div className="space-y-3">{[1,2,3].map((i) => <div key={i} className="h-20 animate-pulse rounded-2xl bg-slate-800/60" />)}</div> : tab === 'commandes' ? <CommandTable rows={filteredCommands} isPatron={isPatron} statusMutation={statusMutation} onDetail={async (row) => { try { setSelected(await achatsApi.getCommande(row.id)); setModal('command-detail'); } catch (e) { notif.error(e?.response?.data?.message || 'Impossible de charger la commande.'); } }} /> : <ReceptionTable rows={filteredReceptions} onDetail={(row) => { setSelected(row); setModal('reception-detail'); }} />}
  {modal === 'new-command' && <Modal title="Nouvelle commande fournisseur" icon={ShoppingCart} onClose={() => !createCommand.isPending && setModal(null)} wide><CommandForm form={cmd} setForm={setCmd} fournisseurs={fournisseurs} articles={articleList} addLine={() => add(setCmd, { articleId: '', quantite: 1, prixAchatUnit: 0 })} updateLine={(i,k,v) => update(setCmd,i,k,v)} removeLine={(i) => remove(setCmd,i)} onSubmit={submitCommand} loading={createCommand.isPending} /></Modal>}
  {modal === 'new-reception' && <Modal title="Nouvelle réception" icon={PackageCheck} onClose={() => !createReception.isPending && setModal(null)} wide><ReceptionForm form={rec} setForm={setRec} fournisseurs={fournisseurs} articles={articleList} addLine={() => add(setRec, { articleId: '', quantiteLivree: 1, quantiteGratuite: 0, prixAchatUnitaire: 0, unite: 'PIECE' })} updateLine={(i,k,v) => update(setRec,i,k,v)} removeLine={(i) => remove(setRec,i)} total={total} debt={debt} onSubmit={submitReception} loading={createReception.isPending} /></Modal>}
  {modal === 'command-detail' && selected && <Modal title={`Commande ${selected.reference}`} icon={ClipboardList} onClose={() => setModal(null)} wide><CommandDetail command={selected} isPatron={isPatron} onEdit={() => setModal('edit-command')} onStatus={(s) => statusMutation.mutate({ id: selected.id, status: s })} loading={statusMutation.isPending} /></Modal>}
  {modal === 'edit-command' && selected && <Modal title={`Modifier ${selected.reference}`} icon={ClipboardList} onClose={() => !updateCommand.isPending && setModal(null)}><EditCommand command={selected} onSubmit={(payload) => updateCommand.mutate({ id: selected.id, payload })} loading={updateCommand.isPending} /></Modal>}
  {modal === 'reception-detail' && selected && <Modal title={`Réception ${selected.reference}`} icon={PackageCheck} onClose={() => setModal(null)} wide><ReceptionDetail reception={selected} /></Modal>}
  </div>;
}

function CommandTable({ rows, isPatron, statusMutation, onDetail }) { return <div className="overflow-hidden rounded-2xl border border-slate-800 bg-slate-900/50">{rows.length ? rows.map((c) => <div key={c.id} className="grid gap-3 border-b border-slate-800/80 px-4 py-4 last:border-0 md:grid-cols-[1.3fr_1fr_0.8fr_0.8fr_1fr] md:items-center"><div><p className="font-semibold text-white">{c.reference}</p><p className="text-xs text-slate-500">{c.lignes?.length || 0} ligne(s)</p></div><p className="text-sm text-slate-300">{c.fournisseur?.nom || '-'}</p><span className={`w-fit rounded-full border px-2.5 py-1 text-xs font-bold ${statusClass[c.statut] || statusClass.BROUILLON}`}>{STATUS[c.statut] || c.statut}</span><p className="text-sm font-bold text-white">{money(c.total)}</p><div className="flex gap-2 md:justify-end"><Button variant="ghost" icon={Eye} onClick={() => onDetail(c)}>Détail</Button>{isPatron && c.statut === 'BROUILLON' && <Button variant="primary" icon={Send} disabled={statusMutation.isPending} onClick={() => statusMutation.mutate({ id: c.id, status: 'ENVOYE' })}>Envoyer</Button>}</div></div>) : <Empty icon={ClipboardList} title="Aucune commande" text="Créez une première commande fournisseur." />}</div>; }
function ReceptionTable({ rows, onDetail }) { return <div className="overflow-hidden rounded-2xl border border-slate-800 bg-slate-900/50">{rows.length ? rows.map((r) => <div key={r.id} className="grid gap-3 border-b border-slate-800/80 px-4 py-4 last:border-0 md:grid-cols-[1.2fr_1fr_0.9fr_0.9fr_1fr] md:items-center"><div><p className="font-semibold text-white">{r.reference}</p><p className="text-xs text-slate-500">{r.numBordereau ? `Bordereau ${r.numBordereau}` : `${r.lignes?.length || 0} ligne(s)`}</p></div><p className="text-sm text-slate-300">{r.fournisseur?.nom || '-'}</p><p className="text-sm text-slate-400">{r.createdAt ? new Date(r.createdAt).toLocaleDateString('fr-FR') : '-'}</p><p className="text-sm font-bold text-amber-300">{money(r.montantDette)}</p><div className="flex gap-2 md:justify-end"><Button variant="ghost" icon={Eye} onClick={() => onDetail(r)}>Détail</Button><Button variant="ghost" icon={ClipboardList} onClick={() => window.print()}>Imprimer</Button></div></div>) : <Empty icon={PackageCheck} title="Aucune réception" text="Enregistrez la première livraison reçue." />}</div>; }
function LineHeader({ title, onAdd }) { return <div className="flex items-center justify-between"><h3 className="text-sm font-bold text-white">{title}</h3><Button variant="ghost" icon={Plus} onClick={onAdd}>Ajouter une ligne</Button></div>; }
function CommandForm({ form, setForm, fournisseurs, articles, addLine, updateLine, removeLine, onSubmit, loading }) { return <div className="space-y-5"><div className="grid gap-4 md:grid-cols-2"><Field label="Fournisseur"><select value={form.fournisseurId} onChange={(e) => setForm((f) => ({ ...f, fournisseurId: e.target.value }))} className="control"><option value="">Sélectionner un fournisseur</option>{fournisseurs.map((f) => <option key={f.id} value={f.id}>{f.nom}</option>)}</select></Field><Field label="Note"><input value={form.note} onChange={(e) => setForm((f) => ({ ...f, note: e.target.value }))} maxLength={500} className="control" /></Field></div><LineHeader title="Articles à commander" onAdd={addLine} />{form.lignes.map((l,i) => <div key={i} className="grid gap-2 rounded-xl border border-slate-800 bg-slate-950/40 p-3 md:grid-cols-[1fr_120px_150px_42px]"><select value={l.articleId} onChange={(e) => updateLine(i,'articleId',e.target.value)} className="control"><option value="">Article</option>{articles.map((a) => <option key={a.id} value={a.id}>{a.designation}</option>)}</select><input type="number" min="1" step="1" value={l.quantite} onChange={(e) => updateLine(i,'quantite',e.target.value)} className="control" placeholder="Qté" /><input type="number" min="0" step="0.01" value={l.prixAchatUnit} onChange={(e) => updateLine(i,'prixAchatUnit',e.target.value)} className="control" placeholder="Prix" /><button type="button" onClick={() => removeLine(i)} className="rounded-xl border border-red-500/20 text-red-300"><Trash2 size={16} className="mx-auto" /></button></div>)}{!form.lignes.length && <Empty icon={Plus} title="Aucune ligne" text="Ajoutez au moins un article." />}<div className="flex justify-end border-t border-slate-800 pt-4"><Button variant="primary" icon={CheckCircle2} disabled={loading || !form.lignes.length} onClick={onSubmit}>{loading ? 'Enregistrement…' : 'Créer la commande'}</Button></div></div>; }
function ReceptionForm({ form, setForm, fournisseurs, articles, addLine, updateLine, removeLine, total, debt, onSubmit, loading }) { return <div className="space-y-5"><div className="grid gap-4 md:grid-cols-2"><Field label="Fournisseur"><select value={form.fournisseurId} onChange={(e) => setForm((f) => ({ ...f, fournisseurId: e.target.value }))} className="control"><option value="">Sélectionner un fournisseur</option>{fournisseurs.map((f) => <option key={f.id} value={f.id}>{f.nom}</option>)}</select></Field><Field label="Mode de paiement"><select value={form.modePaiement} onChange={(e) => setForm((f) => ({ ...f, modePaiement: e.target.value, montantPaye: e.target.value === 'CREDIT' ? 0 : f.montantPaye }))} className="control">{PAYMENTS.map((p) => <option key={p.value} value={p.value}>{p.label}</option>)}</select></Field><Field label="N° bordereau"><input value={form.numBordereau} onChange={(e) => setForm((f) => ({ ...f, numBordereau: e.target.value }))} maxLength={100} className="control" /></Field><Field label="Montant payé"><input type="number" min="0" step="0.01" disabled={form.modePaiement === 'CREDIT'} value={form.montantPaye} onChange={(e) => setForm((f) => ({ ...f, montantPaye: e.target.value }))} className="control" /></Field></div><LineHeader title="Articles reçus" onAdd={addLine} />{form.lignes.map((l,i) => <div key={i} className="grid gap-2 rounded-xl border border-slate-800 bg-slate-950/40 p-3 md:grid-cols-[1.3fr_90px_90px_140px_130px_42px]"><select value={l.articleId} onChange={(e) => updateLine(i,'articleId',e.target.value)} className="control"><option value="">Article</option>{articles.map((a) => <option key={a.id} value={a.id}>{a.designation}</option>)}</select><input type="number" min="1" step="1" value={l.quantiteLivree} onChange={(e) => updateLine(i,'quantiteLivree',e.target.value)} className="control" placeholder="Livrée" /><input type="number" min="0" step="1" value={l.quantiteGratuite} onChange={(e) => updateLine(i,'quantiteGratuite',e.target.value)} className="control" placeholder="Gratuite" /><input type="number" min="0" step="0.01" value={l.prixAchatUnitaire} onChange={(e) => updateLine(i,'prixAchatUnitaire',e.target.value)} className="control" placeholder="Prix" /><select value={l.unite} onChange={(e) => updateLine(i,'unite',e.target.value)} className="control">{UNITS.map((u) => <option key={u} value={u}>{u}</option>)}</select><button type="button" onClick={() => removeLine(i)} className="rounded-xl border border-red-500/20 text-red-300"><Trash2 size={16} className="mx-auto" /></button></div>)}{!form.lignes.length && <Empty icon={Plus} title="Aucune ligne" text="Ajoutez au moins un article." />}<Field label="Note"><textarea value={form.note} onChange={(e) => setForm((f) => ({ ...f, note: e.target.value }))} maxLength={500} rows={3} className="control resize-none" /></Field><div className="grid gap-3 rounded-2xl border border-slate-800 bg-slate-950/40 p-4 sm:grid-cols-3"><Summary label="Total réception" value={money(total)} /><Summary label="Montant payé" value={money(form.montantPaye)} /><Summary label="Dette fournisseur" value={money(debt)} emphasis /></div><div className="flex justify-end border-t border-slate-800 pt-4"><Button variant="success" icon={PackageCheck} disabled={loading || !form.lignes.length} onClick={onSubmit}>{loading ? 'Enregistrement…' : 'Valider la réception'}</Button></div></div>; }
function CommandDetail({ command, isPatron, onEdit, onStatus, loading }) { return <div className="space-y-5"><div className="grid gap-3 sm:grid-cols-4"><Summary label="Fournisseur" value={command.fournisseur?.nom || '-'} /><Summary label="Statut" value={STATUS[command.statut] || command.statut} /><Summary label="Total" value={money(command.total)} /><Summary label="Date" value={command.dateCommande ? new Date(command.dateCommande).toLocaleDateString('fr-FR') : '-'} /></div><div className="overflow-hidden rounded-xl border border-slate-800">{(command.lignes || []).map((l) => <div key={l.id} className="grid grid-cols-[1fr_100px_140px] gap-3 border-b border-slate-800/70 px-4 py-3 text-sm"><span className="text-slate-200">{l.article?.designation || l.articleId}</span><span>{l.quantite}</span><span>{money(l.prixAchatUnit)}</span></div>)}</div>{command.note && <div className="rounded-xl bg-slate-950/40 p-4 text-sm text-slate-400">{command.note}</div>}<div className="flex flex-wrap justify-end gap-2">{command.statut === 'BROUILLON' && <Button icon={ClipboardList} onClick={onEdit}>Modifier</Button>}{isPatron && command.statut === 'BROUILLON' && <Button variant="primary" icon={Send} disabled={loading} onClick={() => onStatus('ENVOYE')}>Envoyer</Button>}{isPatron && command.statut === 'ENVOYE' && <Button variant="success" icon={CheckCircle2} disabled={loading} onClick={() => onStatus('RECU')}>Marquer reçue</Button>}{isPatron && ['BROUILLON','ENVOYE'].includes(command.statut) && <Button variant="warning" icon={X} disabled={loading} onClick={() => onStatus('ANNULE')}>Annuler</Button>}</div></div>; }
function EditCommand({ command, onSubmit, loading }) { const [note, setNote] = useState(command.note || ''); const [date, setDate] = useState(command.dateReceptionPrev ? new Date(command.dateReceptionPrev).toISOString().slice(0,10) : ''); return <div className="space-y-4"><Field label="Date de réception prévue"><input type="date" value={date} onChange={(e) => setDate(e.target.value)} className="control" /></Field><Field label="Note"><textarea rows={4} maxLength={500} value={note} onChange={(e) => setNote(e.target.value)} className="control resize-none" /></Field><div className="flex justify-end"><Button variant="primary" icon={CheckCircle2} disabled={loading} onClick={() => onSubmit({ note, dateReceptionPrev: date ? new Date(`${date}T12:00:00`).toISOString() : undefined })}>{loading ? 'Enregistrement…' : 'Enregistrer'}</Button></div></div>; }
function ReceptionDetail({ reception }) { return <div className="space-y-5"><div className="grid gap-3 sm:grid-cols-4"><Summary label="Fournisseur" value={reception.fournisseur?.nom || '-'} /><Summary label="Paiement" value={PAYMENTS.find((p) => p.value === reception.modePaiement)?.label || reception.modePaiement} /><Summary label="Payé" value={money(reception.montantPaye)} /><Summary label="Dette" value={money(reception.montantDette)} emphasis /></div><div className="overflow-hidden rounded-xl border border-slate-800">{(reception.lignes || []).map((l) => <div key={l.id} className="grid grid-cols-[1fr_90px_90px_120px_120px] gap-3 border-b border-slate-800/70 px-4 py-3 text-sm"><span className="text-slate-200">{l.article?.designation || l.articleId}</span><span>{l.quantiteLivree}</span><span>{l.quantiteGratuite}</span><span>{l.uniteUsed}</span><span>{money(l.prixAchatUnitaire)}</span></div>)}</div>{reception.numBordereau && <p className="text-sm text-slate-400">Bordereau : <span className="font-semibold text-slate-200">{reception.numBordereau}</span></p>}</div>; }
